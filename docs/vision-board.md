# Vision board (shared by the user, 2026-09-26)

Kept so we come back to it once the Home card is done. Pictures are in `docs/mockups/vision/`.

| File | What it is | What the user liked / what to take |
|---|---|---|
| `liked-card-shape.png` | Our own Home card (future day view) | The shape: date on top, big number left, three lights with names and counts right, sentence at the bottom. Use the same shape on the website. |
| `vision-3.png` | Meditation app "Aivo" (dark, warm orange light) | One warm light source glowing from the top of the screen; day chips with a circled active day; one white pill button; an arc score (82/100) with a knob that travels along it; dark cards with small bar charts. |
| `vision-4.png`, `vision-5.png` | VPN app (dark, orange) | One hero object that changes state: grey "Tap to connect" becomes a glowing orange button with an energised particle ring; a big live timer in the accent colour; pill nav of round icon buttons, active one filled. |
| `vision-6.png`, `vision-7.png` | Study app (dark, violet glass) | Frosted glass calendar tiles, today filled with the accent; a time label on the left of a timeline card; floating 3-circle nav; faint grid texture behind content. |
| `vision-8.png`, `vision-9.png`, `vision-10.png` | Baby sleep app "Luli" | Arc timer with moon and alarm knobs; a week row of percentage chips (M to S, 84%, 64%...); glass cards with soft 3D icons; a "Log activity" sheet listing each thing with its last log ("No logs yet") and an arrow; split floating nav (left pill, big centre plus, right pill); premium page with a crown and Yearly / Monthly cards. |

Ideas pulled from it (see chat of 2026-09-26 for the reasoning): log sheet for trackers, week row of percentages, a "day complete" state change for the Home card, split nav with a centre plus, fasting as an arc with a travelling knob, glass calendar tiles on the card back, one ambient light that follows the time of day, a premium page for the paid tier later.

Not to copy: purple AI glow as a default, particle spheres (heavy on phones), 3D emoji icons (Ritsu uses Phosphor line icons and the blob), gradient headline text.

## Notes added 2026-09-26

- `fasting-app-insights.jpg`: the fasting app the user uses today and likes. A long Insights page: consistency %, active days, longest fast, average this month vs last, fasts completed, personal best, weight trend with goal, plateau advice, a 5-week grid of fasting days coloured by length, weekly average fast length bars, a time-of-day heat grid, "phase reach" bars (lipolysis, ketosis, autophagy), estimated fat burned, records, and recent fasts with "Ended early" tags. Use it as the model when we redesign the Fasting page (not built yet). Ritsu never gives medical advice; keep the care note.
- The user is NOT happy with the split bottom menu (two pills with a centre plus): "feels a bit cheap". Kept in the mock for now; revisit the bottom menu design later.
- The user likes the small tick lines under each day in the date strip and wants them clearly visible in both themes.
- Back of the Home card: the user chose "mirror the front across the week" (three strips of seven small LEDs) plus the blob saying the one-line insight.
